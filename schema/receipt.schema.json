{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://heronlabs.com/receipt.schema.json",
  "title": "supera-engineer receipt",
  "description": "Handoff from supera-engineer to the orchestrator. One receipt per implementation run.",
  "type": "object",
  "additionalProperties": false,
  "required": ["summary", "filesChanged", "verification"],
  "properties": {
    "summary": {
      "description": "One-line description of what was implemented.",
      "type": "string",
      "minLength": 1
    },
    "filesChanged": {
      "description": "Paths of files modified or created, relative to repo root.",
      "type": "array",
      "items": {"type": "string"},
      "minItems": 1
    },
    "verification": {
      "description": "Verification output per layer. Keys: build, lint, plus one per testCommands entry. Values: pass, fail, skipped.",
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "enum": ["pass", "fail", "skipped"]
      },
      "minProperties": 1
    }
  }
}
