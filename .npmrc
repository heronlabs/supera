# Pin the public registry so Dependabot resolves public deps from npmjs, not the
# org's GitHub Packages base (whose @types/node mirror is stale at 17.0.18).
registry=https://registry.npmjs.org
auto-install-peers=true
fund=false
